Creating custom root fs image :
--------------------------------
1. Place your files in /root directory

2. Run build.sh to create the root fs image (root.img)

	$./build.sh

Note : 
------
Do not remove the below files,
	part.json
	boot.json
	fota_config.json
	dirty

Any other custom files can be added deleted as per requirement 
